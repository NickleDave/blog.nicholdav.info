<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.1">Jekyll</generator><link href="https://blog.nicholdav.info//feed.xml" rel="self" type="application/atom+xml" /><link href="https://blog.nicholdav.info//" rel="alternate" type="text/html" /><updated>2023-01-03T15:23:41-05:00</updated><id>https://blog.nicholdav.info//feed.xml</id><title type="html">Lifelong Learning Machine</title><subtitle>Research software engineer with expertise in artificial intelligence and computational cognitive neuroscience</subtitle><author><name>David Aaron Nicholson</name><email>nicholdav@gmail.com</email></author><entry><title type="html">Four tips for structuring your research group’s Python packages</title><link href="https://blog.nicholdav.info//four-tips-structuring-research-python/" rel="alternate" type="text/html" title="Four tips for structuring your research group’s Python packages" /><published>2022-12-26T00:00:00-05:00</published><updated>2022-12-26T00:00:00-05:00</updated><id>https://blog.nicholdav.info//four-tips-structuring-research-python</id><content type="html" xml:base="https://blog.nicholdav.info//four-tips-structuring-research-python/"><![CDATA[<p>This is one of those petulant posts from someone who writes software.
A post from one of those people that describes some library they wrote as “opinionated”,
as if the rest of us have been living in fear of expressing our opinions through software.</p>

<p>So, sorry in advance.
But I want to talk about some things I see a lot in Python packages shared by research groups.</p>

<p>At the highest level, there’s two things I see:
one, researchers name their modules in a way that
might make sense when you’re writing your own code,
but can seem very verbose or non-intuitive
when you are someone else on the outside
reading or using that code.
Two, I see a lot of code that doesn’t
structure itself in a way
that makes it easier for people to read it and to use it.
And you’re sharing your code because you want people to use it, right?</p>

<p>I think a lot of packages from research labs end up this way 
in part when researchers don’t have a good 
<a href="https://teachtogether.tech/en/#s:models">mental model</a> 
of the mapping from their written code to the package they import.
In other words, they’re not thinking 
of how Python translates 
the directory structure of their code and its internal <code class="language-plaintext highlighter-rouge">import</code> statements
into a package name with modules that can be accessed with the dot operator,
like so: <code class="language-plaintext highlighter-rouge">package.subpackage.module.function</code>.</p>

<p>To use a technical term, 
the code does not fully leverage its own <a href="https://realpython.com/python-namespaces-scope/">namespace</a>,
the literal mapping from names of things in the package 
to the functions and classes that they refer to.
If I may invoke the holy sayings of the revered masters,
<a href="https://inventwithpython.com/blog/2018/08/17/the-zen-of-python-explained/">“namespaces are a honking great idea”</a>.
To a wizened software engineer, 
that phrase from the Zen of Python might sound 
like Python is patting itself on the back
for something that basically every modern language does.
But for many researchers that are getting their first experience designing a software library, 
it might seem surprising that I’m drawing a connection between 
the pile of files that constitues their code and an abstraction from computer science.
If it sounds surprising to you, 
maybe it’s because you don’t (yet) have a good <a href="https://teachtogether.tech/en/#s:memory-concept-maps">concept map</a> 
of how namespaces relate to naming your modules and structuring your package.</p>

<p>So let me tell you a little bit about that, 
and how you can build on it to make your code much more accessible.
I don’t claim to have a perfect mental model of Python packages,
but at least I have fought enough with my own code,
and read enough of others peoples’ code, that I think I can say something useful here.
This post contains some things I’ve learned the hard way,
to hopefully save you from repeating the mistakes of past me.</p>

<p>I’ve boiled it down to four tips, that I’ll explain in more detail below.</p>

<p>Here’s the high-level version:</p>

<ol>
  <li>Give your packages and modules terse, single-word names whenever possible.</li>
  <li>Import modules internally, instead of importing everything <em>from</em> modules.</li>
  <li>Make use of sub-packages.</li>
  <li>Prefer modules with very specific names containing single functions over modules with very general names like <code class="language-plaintext highlighter-rouge">utils</code>, <code class="language-plaintext highlighter-rouge">helpers</code>, or <code class="language-plaintext highlighter-rouge">support</code> that contain many functions.</li>
</ol>

<p>Of course, it goes without saying that these are absolute rules
that came to me in a dream where Guido Van Rossum
emerged from a burning bush and
handed them to me on a freshly-minted <a href="https://peps.python.org/pep-0000/">PEP</a>.
As such, you should never violate them for any reason,
unless you feel like it.
(Those last two sentences should be written in the sarcasm font.)
More seriously, you could call these rules, 
but there are sometimes good reasons to not follow them to the letter, 
like we’ll see below.</p>

<p>One other thing to say before we get started: 
I will use some examples here inspired by real life,
but namespaces have been changed to protect the innocent.
I promise my goal here is not to name shame,
but instead to help you make your code as accessible as possible.
I also want to note this is <em>not</em> meant to be a whole tutorial,
just some pointers on how to pick module names
and how to structure your package in a way that makes your code
more immediately accessible and more readable.
For an introductory tutorial to packages and modules,
please see <a href="https://realpython.com/python-modules-packages/">https://realpython.com/python-modules-packages/</a>.</p>

<h2 id="an-example-of-what-not-to-do">An example of what <em>not</em> to do</h2>

<p>Here’s the scenario:
You are really into sharing your research code written in Python.
Great! Sharing code just makes sense to you.
Your lab is getting on board with this too.
You made a
<a href="https://docs.github.com/en/organizations/collaborating-with-groups-in-organizations/about-organizations">GitHub organization</a>
for the research group
(instead of making an individual profile with the lab name!
Please don’t do that, and make an org page instead,
so we can see who’s in your group and understand who contributes what!
For when I want to write you an email thanking you for your work,
or ask you to collaborate,
or figure out who I want to hire away from academia into the private sector 😈).</p>

<p>Now everybody in the lab is forking their repos to that org,
so you can collaborate and all rely on the same version of the same code.
I go to your lab’s org page and check out one of the repos, and I see something like this.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>electroymyographytoolkit
├── emg_load_inchan_data_four_channelinkls.py
├── emg_main.py
├── emg_simulate.py
├── emg_solver.py
├── emg_utils.py
└── __init__.py

</code></pre></div></div>

<p>In the words of a wise man:</p>

<blockquote>
  <p>Oh No Baby! What Is You Doin???</p>
</blockquote>

<p class="small"><cite> — <a href="https://knowyourmeme.com/memes/oh-no-baby-what-is-you-doin"><code class="language-plaintext highlighter-rouge">nicknpattiwhack_</code></a></cite></p>

<h2 id="what-you-should-do-instead">What you should do instead</h2>

<p>Please! Help as many people as possible actually use your code
by avoiding naming and package structures like that!
Instead you want something like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>emgtoolkit
├── load.py
├── main.py
├── simulate.py
├── solvers
│   ├── brute_force.py
│   ├── entropy.py
│   ├── gibson.py
│   ├── __init__.py
│   ├── random_walk.py
│   └── shannon.py
└── timestamp.py
</code></pre></div></div>

<p>The changes I made above follow the aforementioned four tips.
Here’s they are again, the short version of what you want to do:</p>

<ol>
  <li>Give your packages and modules terse, single-word names whenever possible.</li>
  <li>Import modules internally, instead of importing everything <em>from</em> modules.
(You can’t see this directly in the example above, but I’ll show it below.)</li>
  <li>Make use of sub-packages.</li>
  <li>Prefer modules with very specific names containing single functions over modules with very general names like <code class="language-plaintext highlighter-rouge">utils</code>, <code class="language-plaintext highlighter-rouge">helpers</code>, or <code class="language-plaintext highlighter-rouge">support</code> that contain many functions.</li>
</ol>

<p>All of these rules help you make code written with your package more readable from the <em>outside</em>,
when someone else uses it from a script.
Rules 2,3, and 4 in particular can also help make your code more readable from the <em>inside</em>,
when you revisit it
after you spent six months in the lab fighting with weird noise from your electrophysiology rig
or an IHC protocol or whatever it is that scientists do, I don’t remember anymore
([laughs in post-academic]).</p>

<h2 id="please-dont-name-your-modules-packagepkg_module">Please don’t name your modules <code class="language-plaintext highlighter-rouge">package.pkg_module</code></h2>

<p>Okay, I will briefly, just for one paragraph, get really “opinionated”.
I am not yelling at you. I’m yelling <em>with</em> you,
from the perspective of you as a future user and reader of your own code.
(Borrowing a concept from physics while simultaneously breaking its laws,
we change our frame of reference and do some time travel
so that what you named your modules is <em>no longer</em> a problem for “future you” to solve.)</p>

<p><strong>yelling</strong>:<br />
No one wants to type out <code class="language-plaintext highlighter-rouge">package.pkg_solver</code>!
Why are you making me redundantly type the package name again <em>after I just typed it</em>?
Only instead you’re making me <em>remember</em> and type some abbreviated version of the package name
that you dropped some vowels from arbitrarily?
<em>And</em>, <strong>and</strong> you’re making me type an underscore,
that my hand has to go find the shift key for! Every time!!!
Yes I know that tab complete exists! This is still too much typing!!! Why do you hate me?</p>

<p>More calmly:<br />
That might seem like a weirdly specific example to you, 
but I have seen naming schemes like this more than once (hence, this post). 
I really do think it has something to do with the way other researchers are thinking about 
code while writing it and when importing it. 
They want to import a module <em>from</em> their package and know where it came from, 
so they put the package name in the module name. I guess? :shrug:
Instead of doing anything like that, 
just name your package and modules something terse from the get-go, like</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>emgtoolkit
├── load.py
├── main.py
├── simulate.py
├── solvers
│   ├── brute_force.py
│   ├── entropy.py
│   ├── gibson.py
│   ├── __init__.py
│   ├── random_walk.py
│   └── shannon.py
└── timestamp.py
</code></pre></div></div>

<p>Now in your scripts you can write out the whole package name, 
while at the same time having it be more succinct and clear what part of the 
package’s namespace you’re referring to.
E.g., <code class="language-plaintext highlighter-rouge">emgtoolkit.simulate</code> instead of <code class="language-plaintext highlighter-rouge">electromyographytoolkit.emg_simulate</code>.
You might have noticed some other things changed in the names, too.
Those changes relate to the other tips, that I’ll say more about below.</p>

<h2 id="please-dont-name-your-modules-packageincredibly_long_technical_name_or_specific_task_separated_by_underscores">Please don’t name your modules <code class="language-plaintext highlighter-rouge">package.incredibly_long_technical_name_or_specific_task_separated_by_underscores</code></h2>

<p>There is also an even more extreme form of verbose module names, that takes the form of:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pkg/
  incredibly_long_technical_name_separated_by_underscores
</code></pre></div></div>

<p><em>Sometimes</em> this is justified, if it’s the core functionality of your package and you really need to clearly communicate to everyone in the universe what the package does. E.g., <code class="language-plaintext highlighter-rouge">sklearn.model_selection</code> has a very specific meaning, and it’s hard to capture with an abbreviation, and the library is trying to capture basically several sub-fields of machine learning within this sub-package. I am okay with tab-completing this name just so it’s clear to all of us what the heck we’re doing. But <code class="language-plaintext highlighter-rouge">pkg.preprocessing_electrophysiological_data</code> is not a great name. Especially if your package <em>only</em> deals with electrophysiological data. You can just say <code class="language-plaintext highlighter-rouge">pkg.preprocess</code>, it’s fine.</p>

<h3 id="instead-make-module-names-general-and-move-specifics-to-function-names">Instead, make module names general and move specifics to function names</h3>

<p>Often these really long module names can be a very specific thing you need to do.
Here again you can make things more terse
by moving the more specific part to a function name,
while keeping the more general part as a module name.
For example, instead of <code class="language-plaintext highlighter-rouge">electromyographytoolkit.emg_load_inchan_data_four_channels</code>,
name the module <code class="language-plaintext highlighter-rouge">load</code>,
and name a function in that module <code class="language-plaintext highlighter-rouge">inchan_data_four_channels</code>,
so that you can type out <code class="language-plaintext highlighter-rouge">emgtoolkit.load.inchan_data_four_channels</code>.
Your intention is still obvious but you rely on the dot attribute access to make that intention clear,
yet simultaneously more succinct.
This also means you have left yourself room later to add other <code class="language-plaintext highlighter-rouge">load</code> functions in your modulle,
like <code class="language-plaintext highlighter-rouge">inchan_data_accelerometer_channel</code>, or whatever.
Trust me, there <em>will</em> be a later.
Leave <em>space</em> in your namespace.</p>

<h2 id="import-modules-not-functions-and-classes-for-readability">Import modules, not functions and classes, for readability</h2>

<p>This is less about naming and more about importing,
but it’s related because I feel like this same anti-pattern arises
from someone not understanding what it is that package structure gives you.
It gives you a logical grouping that’s readable 
not just from the outside <em>but also from the inside</em>.
Let me explain.</p>

<p>I often see something like this in an internal module:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># inside main.py
from pkg_solver import (
    EntropySolver,
    ShannonSolver,
    GibsonSolver,
    ...
    # 10 more lines of class imports from a single module (!)
    BruteForceSolver,
    RandomWalkSolver
)
</code></pre></div></div>

<p>Let’s say this is in <code class="language-plaintext highlighter-rouge">emgtoolkit/main.py</code>.
Going back to the overall goal of this post,
let’s think about how the import above will 
affect the namespace of your package, as seen from the outside.
if I am using your package,
one of the first things I’m going to do is inspect
the content of  <code class="language-plaintext highlighter-rouge">emgtoolkit.main</code>, 
e.g. by running <code class="language-plaintext highlighter-rouge">dir(emgtoolkit.main)</code> in a Python REPL, 
since that name signals to me very loudly 
that you think of this as its main interface or API.
With an import statement like the one above,
when I tab complete while working with this module,
I am going to be faced with a wall of <code class="language-plaintext highlighter-rouge">Solver</code> classes
that obscures the true function of the module.
This is not the <code class="language-plaintext highlighter-rouge">solver</code> module! That’s some other module!
If this is a high-level module that you expect me to access as a user,
then please avoid polluting it with all these names.
One of the ways you can do this is by making use of sub-packages,
as I’ll talk about below.</p>

<p>I want to make a second point here,
that is less about making external use of your code readable,
and more about making it readable <em>internally</em>, within the code base.
I know that right now while you are hacking on the code,
you feel like you have all the low-level context about what all the moving parts are doing.
But if you want to make life easier for yourself six months from now,
and easier for everyone else that has to use your code,
write in a way that <em>surfaces</em> that context.
Write in a way that makes it much more obvious where all the functions and classes come from,
leveraging the logical grouping that you spent so much time boiling down into concise module names.</p>

<p>Please, for your future self and everyone else, make the code readable by importing <em>just</em> the module,
and then referring to those classes (or function or whatever you’re referring to)
via attribute access, i.e. with a dot, like <code class="language-plaintext highlighter-rouge">solver.BruteForceSolver</code>.
You can get rid of many lines of imports at the top of your module
<em>and</em> help yourself out when you are reading many lines deep in your own module,
far from those imports, if you instead do this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># just import one thing, don't pollute namespace
from . import solvers

...
# ~150 lines in
    if solve_strategy == "entropy"
        # ah, this is one of many solvers in this
        solver = solvers.EntropySolver()
    elif solve_strategy == "shannon"
        solver = solvers.ShannonSolver()
</code></pre></div></div>

<p>One slight drawback of this terse approach is you can end up shadowing the most intuitive variable name for a class instance with a module that has a similar name. I.e., if I rename my module <code class="language-plaintext highlighter-rouge">pkg_solver</code> -&gt; <code class="language-plaintext highlighter-rouge">solver</code> then I won’t be able to do <code class="language-plaintext highlighter-rouge">from . import solver</code> at the top and then later <em>also</em> say <code class="language-plaintext highlighter-rouge">solver = solver.EntropySolver()</code>, because the variable name <code class="language-plaintext highlighter-rouge">solver</code> representing my class instance will clobber the name in the namespace that represents my <code class="language-plaintext highlighter-rouge">solver</code> module.
This will cause Python’s head to explode (and yours). In this case I prefer to either use an absolute import internally, <code class="language-plaintext highlighter-rouge">import pkg.solver</code>, or to alias internally, e.g., <code class="language-plaintext highlighter-rouge">from . import solver as solver_classes</code>, so that an <em>external</em> user <em>still</em> can write something simple like <code class="language-plaintext highlighter-rouge">pkg.solver</code>.</p>

<h2 id="you-should-know-sub-packages-exist">You should know sub-packages exist</h2>

<p>Again, this is less about naming, and more about structuring internals.
But it falls under the same category of “thing I see that tells me you might not yet have a mental model of how to organize your code to make it easier for you to maintain in the long run”.</p>

<p>You should know (if you don’t already) that you can make a sub-package inside your package.</p>

<p>See this example in the Python docs on modules: <a href="https://docs.python.org/3/tutorial/modules.html#packages">https://docs.python.org/3/tutorial/modules.html#packages</a></p>

<p>Recalling our example from above, we can refactor our (presumably gigantic) <code class="language-plaintext highlighter-rouge">solver</code> module 
into a handful of modules inside a sub-package:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>emgtoolkit
├── load.py
├── main.py
├── simulate.py
├── solvers
│   ├── brute_force.py
│   ├── entropy.py
│   ├── gibson.py
│   ├── __init__.py
│   ├── random_walk.py
│   └── shannon.py
└── timestamp.py
</code></pre></div></div>

<p>Then you can use imports so that, from the perspective of the user, 
they have access to the <em>same</em> modules
with classes, functions, etc., that they had before.
You do this by importing all the classes from each module in the sub-package inside the <code class="language-plaintext highlighter-rouge">__init__.py</code>
of that sub-package.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"""solver sub-package"""
# this is __init__.py
from .entropy_solver import EntropySolver
from .shannon_solver ShannonSolver
from .gibson_solver GibsonSolver
...
</code></pre></div></div>

<p>This kind of looks like the thing I told you to <em>not</em> do above.
But the difference here is that you are importing inside the <code class="language-plaintext highlighter-rouge">__init__.py</code> file,
where (ideally) you are not hiding any functions;
instead you are just doing some boring boilerplate imports here
to make your life easier somewhere else.
This lets you be more intentional about your API too.
You can have some helper functions inside the sub-package modules that users don’t really need,
and to broadcast this to the world,
you just import the stuff you think people need inside your sub-package <code class="language-plaintext highlighter-rouge">__init__.py</code></p>

<div class="notice">
Python doesn't have a concept of "public and private members of objects",
as languages like Java do,
so there's no (good) way for you to **absolutely** prevent someone from
importing and accessing things.
Instead Python follows the principle of "we're all adults here",
and uses some conventions to indicate things like private and internal functions.
See 
<a href="https://peps.python.org/pep-0008/#descriptive-naming-styles">the section of PEP8 on naming</a>,
in particular, where it talks about <tt>_single_leading_underscore</tt> 
as a weak “internal use” indicator. 
</div>

<p>But from your perspective as a maintainer, you can easily focus on one little chunk of code at a time.
<em>I promise</em> that in the long run, this will help you,
especially when you need to context switch and work on multiple things at once.
<em>Especially</em> if there’s some helper functions and you’re not having to scroll
or jump around through 3000 lines of code to find them.</p>

<h2 id="finally-dont-name-a-module-packageutils-or-packageutilities">Finally, don’t name a module <code class="language-plaintext highlighter-rouge">package.utils</code> or <code class="language-plaintext highlighter-rouge">package.utilities</code></h2>

<p>Someone even more jaded than me has already said this:
<a href="https://breadcrumbscollector.tech/stop-naming-your-python-modules-utils/">https://breadcrumbscollector.tech/stop-naming-your-python-modules-utils/</a></p>

<p>The basic idea is that the concept of <code class="language-plaintext highlighter-rouge">utilities</code> is so nebulous that more and more things will end up crammed into this module, that are less and less related to each other.
This is especially if multiple people are developing a package.
 and they are tempted by this name to just cram that one helper or validation function 
 inside <code class="language-plaintext highlighter-rouge">utilities</code>.</p>

<p>Hold on, friend. Take a step back, take a deep breath. 
It’s okay to add a module with just a single function. 
You can just have a module named <code class="language-plaintext highlighter-rouge">timestamp</code> with only one function <code class="language-plaintext highlighter-rouge">get_timestamp</code>, 
and have it be only 10 lines. Please believe me, it will make the codebase easier to read, 
it will make internal usage of the function easier to read, 
it will make it easier to write quick unit tests. 
You do <strong>not</strong> want to hold 500 lines of <code class="language-plaintext highlighter-rouge">utils</code> in your head when you’re trying to track down a bug.</p>

<p>So that’s me saying what that other blog post said, after I said they already said it.
All I want to add here is that the same logic from above applies: 
you <em>want</em> people to use your package.
So <em>give</em> the modules <em>very specific</em> names so people know exactly what everything is doing.
It will help your potential users and it will help future you.</p>

<h2 id="take-homes">Take-homes</h2>

<p>At the risk of repeating myself, 
let me tell you again why you’d want to take these tips to heart.
You want people to use your library that you’re sharing with the world.
It’s amazing; people can go onto the internet 
and just <em>use</em> the tools you put all that effort into!
As a researcher, 
this is the kind of instant gratification you can’t get 
when you are taking on the Herculean task 
of putting reality into a headlock with an experiment.</p>

<p>So make the most of it!
Give your Python packages and modules names with just 
a few letters, and make use of imports and sub-packages 
to further structure the namespace of your package 
so that it’s convenient to work with.
This will also benefit you 
in the long run when you’re working with your own code.</p>

<p>Since I’m writing for research scientists, 
I will of course end the discussion 
by claiming that this blog sets the stage 
for future research.
A good question would be, 
has anybody done any research 
supporting my claims that these tips will help 
you and others read and use your code?
If I ever finish reading 
<a href="https://www.manning.com/books/the-programmers-brain">The Programmer’s Brain</a>,
I’ll probably be able to tell you.
Stay tuned for another post. 🙂</p>]]></content><author><name>David Aaron Nicholson</name><email>nicholdav@gmail.com</email></author><summary type="html"><![CDATA[This is one of those petulant posts from someone who writes software. A post from one of those people that describes some library they wrote as “opinionated”, as if the rest of us have been living in fear of expressing our opinions through software.]]></summary></entry><entry><title type="html">Make your own `academicpages` site from scratch with `jekyll`</title><link href="https://blog.nicholdav.info//how-to-use-jekyll-to-set-up-your-own-academic-website/" rel="alternate" type="text/html" title="Make your own `academicpages` site from scratch with `jekyll`" /><published>2022-03-16T00:00:00-04:00</published><updated>2022-03-16T00:00:00-04:00</updated><id>https://blog.nicholdav.info//how-to-use-jekyll-to-set-up-your-own-academic-website</id><content type="html" xml:base="https://blog.nicholdav.info//how-to-use-jekyll-to-set-up-your-own-academic-website/"><![CDATA[<p>Update 2022-12-17: I added a home page and a blog to my site,
so I no longer use a set up exactly like the academicpages theme,
but I’m leaving this post up to document the process
(and because it’s currently my only post :sob:)</p>

<p>In this post I document the process I went through
to set up a peronsal web site with a structure
similar to the academicpages theme
used by many academics (<a href="https://academicpages.github.io/">https://academicpages.github.io/</a>).
I’m writing the post for my future self,
and for you if you want to do the same.</p>

<h2 id="step-by-step-guide-to-developing-your-own-academicpages">Step-by-step guide to developing your own ‘academicpages’</h2>
<p>Here’s the summary version first, so you know what you’re getting into:</p>
<ol>
  <li>set up a <code class="language-plaintext highlighter-rouge">jekyll</code> development environment</li>
  <li>make the landing page be your about page</li>
  <li>make the sidebar show your profile with links</li>
  <li>add links to publications, talks, etc., in the nav bar at the top of the page</li>
</ol>

<h2 id="why-would-you-bother">Why would you bother?</h2>

<p>I wanted a site similar in style to <code class="language-plaintext highlighter-rouge">academicpages</code>
(<a href="https://academicpages.github.io/">https://academicpages.github.io/</a>),
with links in the sidebar to Google Scholar, OrcID, etc.,
and the ability to use <code class="language-plaintext highlighter-rouge">jekyll</code>’s “collections” feature
to easily organize publications, talks and teaching.
But I also wanted the flexibility to change themes,
which is not easy to do with <code class="language-plaintext highlighter-rouge">academicpages</code>.</p>

<p>If you’re like me, and you mainly write code in Python,
you might naturally use a static site generator
written in Python (as I did for a while, see next section).
Using jekyll, that’s written in Ruby, might feel like a big jump–
the big pain point in switching languages is often
getting familiar with their tooling.
As a Pythonista, of course I want to support Python libraries,
and some of the static site generators like <code class="language-plaintext highlighter-rouge">pelican</code>
are battle tested with themes that are very versatile,
like their version of
<a href="https://github.com/getpelican/pelican-themes/tree/master/pelican-bootstrap3"><code class="language-plaintext highlighter-rouge">bootstrap</code></a>.
But somehow <code class="language-plaintext highlighter-rouge">minimal-mistakes</code> and other <code class="language-plaintext highlighter-rouge">jekyll</code> themes just look better
to my (untrained) eye.</p>

<p>And as a programmar and developer, it might be a fun exercise
in using libraries in a language besides Python.
It’s interesting to experience the tooling in the ecosystems
of other programming languages.</p>

<h2 id="how-did-we-get-here">How did we get here?</h2>

<p>I’ve had a personal web page for sometime that I set up with
<code class="language-plaintext highlighter-rouge">pelican</code>, a static web page generator in Python.
Basically I adopted my approach from the one Jake Vanderplas took for his
<a href="https://github.com/jakevdp/jakevdp.github.io-source/">“pythonic perambulations” blog</a>.
However I wasn’t happy with how my page looked
(which has nothing to do with Jake Vanderplas,
and everything to do with my moribund graphic design and css/html skillz).
And it was time to update my site anyway.
Recently I noticed a collaborator had set up their own personal site using the
excellent <a href="https://academicpages.github.io/"><code class="language-plaintext highlighter-rouge">academicpages</code></a> repository,
developed by <a href="https://stuartgeiger.com/">Stuart Geiger</a>.
<code class="language-plaintext highlighter-rouge">academicpages</code> is great if you are trying to survive in academia
and you don’t have time to bother learning how to use a static site generator.
With <code class="language-plaintext highlighter-rouge">academicpages</code> you just want to follow a couple steps to add your own
content to some existing template, to easily get a site.</p>

<p>Some background:
<code class="language-plaintext highlighter-rouge">academicpages</code> is a fork of <a href="https://mmistakes.github.io/minimal-mistakes/"><code class="language-plaintext highlighter-rouge">minimal-mistakes</code></a>,
a very popular theme for the <code class="language-plaintext highlighter-rouge">jekyll</code> library.
<code class="language-plaintext highlighter-rouge">jekyyl</code> is a static site generator written in
the <code class="language-plaintext highlighter-rouge">ruby</code> programming language.
You can think of it as analogous to <code class="language-plaintext highlighter-rouge">pelican</code> in Python
(or Ruby developers might tell me that <code class="language-plaintext highlighter-rouge">pelican</code> is analgous to <code class="language-plaintext highlighter-rouge">jekyll</code>).
One advantage of using <code class="language-plaintext highlighter-rouge">jekyll</code> is that
GitHub really likes <code class="language-plaintext highlighter-rouge">jekyll</code>,
letting developers easily create web pages for themselves or documentation
for their projects through the
<a href="https://docs.github.com/en/free-pro-team@latest/github/working-with-github-pages/about-github-pages-and-jekyll">GitHub Pages service</a>.
And it’s free, another plus for academics.
Stuart modified the <code class="language-plaintext highlighter-rouge">minimal-mistakes</code> theme in such a way
that other academics could simply fork its repo
on GitHub and use the web interface to add their own content,
without needing to know how to use <code class="language-plaintext highlighter-rouge">jekyll</code>
or how to use any developer tools like <code class="language-plaintext highlighter-rouge">git</code>.</p>

<p>So I knew I wanted a site like <code class="language-plaintext highlighter-rouge">academicpages</code>,
but I also knew there were things I wanted to modify, such as the theme / template.
There’s little documentation in the <code class="language-plaintext highlighter-rouge">academicpages</code> guide about
<em>what</em> changes were made to the <code class="language-plaintext highlighter-rouge">minimal-mistakes</code> theme, and why.
How did Stuart evolve the template from your standard <code class="language-plaintext highlighter-rouge">jekyll</code>
minimal blog page to a very specific set-up for academics?
Unique features of <code class="language-plaintext highlighter-rouge">academicpages</code> include
that it lands on the <code class="language-plaintext highlighter-rouge">about</code> page and shows a profile in the sidebar
with links to academia specific sites, and
includes tabs like “publications”, “talks”, etc.?
I tried looking at the <code class="language-plaintext highlighter-rouge">git</code> commit history but couldn’t
piece it together.
I figured there couldn’t be <em>too too</em> many steps
to setting up a similar site on my own.</p>

<h2 id="step-by-step-guide-to-developing-your-own-academicpages-1">Step-by-step guide to developing your own ‘academicpages’</h2>
<p>Again, here’s the outline of what we’re going to do:</p>
<ol>
  <li>set up a <code class="language-plaintext highlighter-rouge">jekyll</code> development environment</li>
  <li>make the landing page be your about page</li>
  <li>make the sidebar show your profile with links</li>
  <li>add links to publications, talks, etc., in the nav bar at the top of the page</li>
</ol>

<h3 id="setting-up-a-development-environment">Setting up a development environment</h3>
<p>This is a thing that might be new to academics
not familiar with software engineering,
but the <code class="language-plaintext highlighter-rouge">jekyll</code> and <code class="language-plaintext highlighter-rouge">ruby</code> devs have worked really hard
to make it easy.</p>

<ul>
  <li>set up a basic environment for working with <code class="language-plaintext highlighter-rouge">jekyll</code>
    <ul>
      <li>their quickstart is here: <a href="https://jekyllrb.com/docs/">https://jekyllrb.com/docs/</a></li>
      <li>I’m on PopOS, a Linux distribution similar to Ubuntu, and I use oh-my-zsh,
so I followed these instructions: <a href="https://jekyllrb.com/docs/installation/ubuntu/">https://jekyllrb.com/docs/installation/ubuntu/</a>
except I changed <code class="language-plaintext highlighter-rouge">.bashrc</code> to <code class="language-plaintext highlighter-rouge">.zshrc</code></li>
    </ul>
  </li>
  <li>then I followed the directions for managing dependencies with <code class="language-plaintext highlighter-rouge">bundler</code>
so that I could install the <code class="language-plaintext highlighter-rouge">minimal-mistakes</code> template as a <code class="language-plaintext highlighter-rouge">gem</code>
instead of forking their repo and modifying it
    <ul>
      <li><a href="https://jekyllrb.com/tutorials/using-jekyll-with-bundler">https://jekyllrb.com/tutorials/using-jekyll-with-bundler</a></li>
    </ul>
  </li>
</ul>

<h3 id="making-the-landing-page-be-your-about-page">Making the “landing page” be your “about” page</h3>
<p>This is the first thing that’s specific to our site design.</p>

<ul>
  <li>use the <code class="language-plaintext highlighter-rouge">redirect</code> plugin so that the site lands on “about”</li>
  <li>add an <code class="language-plaintext highlighter-rouge">about.md</code> –
make sure you copy the <a href="https://jekyllrb.com/docs/front-matter/">YAML front matter</a>
from <code class="language-plaintext highlighter-rouge">academicpages</code></li>
  <li>make sure <code class="language-plaintext highlighter-rouge">_config.yml</code> specifies defaults for <code class="language-plaintext highlighter-rouge">pages</code> so that your
theme gets applied to this <code class="language-plaintext highlighter-rouge">about.md</code> file
    <ul>
      <li>I copied the <code class="language-plaintext highlighter-rouge">#defaults</code> section from the <code class="language-plaintext highlighter-rouge">academicpages</code> <code class="language-plaintext highlighter-rouge">_config.yml</code></li>
    </ul>
  </li>
</ul>

<h3 id="making-the-sidebar-show-your-profile-with-links">Making the sidebar show your profile, with links</h3>
<p>Now we’re cooking with <code class="language-plaintext highlighter-rouge">academicpages</code> gas! Or something</p>

<ul>
  <li>to make the author profile in the sidebar render all the links that are
in <code class="language-plaintext highlighter-rouge">academicpages</code> but not in <code class="language-plaintext highlighter-rouge">minimal-mistakes</code>, i.e. academia specific
things like a Google Scholar page, ORCID profile, etc., you’ll need to add
an <code class="language-plaintext highlighter-rouge">author-profile.html</code> in an <code class="language-plaintext highlighter-rouge">_includes</code> directory.
Basically I copied the <code class="language-plaintext highlighter-rouge">author-profile.html</code> from <code class="language-plaintext highlighter-rouge">minimal-mistakes</code> and
then added the relevant if-then statements in the <code class="language-plaintext highlighter-rouge">author-links</code> section.</li>
  <li>for things like an author pic, I added them in <code class="language-plaintext highlighter-rouge">assets/images/</code> – I think
this is consider “idiomatic Jekyll” even though the <code class="language-plaintext highlighter-rouge">academicpages</code> repo
just has an <code class="language-plaintext highlighter-rouge">images</code> folder in its root. Doesn’t matter too much as long
as you specify the correct relative path in your <code class="language-plaintext highlighter-rouge">_config.yml</code></li>
</ul>

<h3 id="adding-links-to-publications-talks-etc-in-the-masthead-nav-bar">Adding links to publications, talks, etc., in the masthead nav bar</h3>
<p>Unique to <code class="language-plaintext highlighter-rouge">academicpages</code> are the links in the navigation bar
at the top of the page to pages with lists of publications, talks,
and so on.</p>

<p>If you want the same thing, there’s two things you’ll need to do:</p>
<ol>
  <li>you’ll need to copy the ‘navigation.yml’ file from <code class="language-plaintext highlighter-rouge">academicpages</code>,
placing it in a <code class="language-plaintext highlighter-rouge">data/</code> subfolder in your project root,
and modify it as you see fit.</li>
  <li>add two related sections in your <code class="language-plaintext highlighter-rouge">_config.yml</code>:
    <ul>
      <li>a ‘collections’ section,
 like <a href="https://github.com/academicpages/academicpages.github.io/blob/25c30de2b4ce3e3f23559384699bb4b9865d6473/_config.yml#L176">this one</a>
 in the <code class="language-plaintext highlighter-rouge">academicpages</code> <code class="language-plaintext highlighter-rouge">_config.yml</code>.</li>
      <li>and corresponding keys in your <code class="language-plaintext highlighter-rouge">defaults</code> section
 as shown <a href="https://github.com/academicpages/academicpages.github.io/blob/25c30de2b4ce3e3f23559384699bb4b9865d6473/_config.yml#L212">here</a>
 in the <code class="language-plaintext highlighter-rouge">academicpages</code> <code class="language-plaintext highlighter-rouge">_config.yml</code>.</li>
    </ul>
  </li>
</ol>

<p>This <em>is</em> explained very briefly in the <code class="language-plaintext highlighter-rouge">about.md</code> of <code class="language-plaintext highlighter-rouge">academicpages</code> but
here’s a quick explainer of how it all works.
<code class="language-plaintext highlighter-rouge">minimal-mistakes</code> lets you add links to the masthead
by supplying a file called <code class="language-plaintext highlighter-rouge">navigation.yml</code>.
This is a
<a href="https://jekyllrb.com/tutorials/navigation/">standard approach in <code class="language-plaintext highlighter-rouge">jekyll</code></a>.
There are other uses for <a href="https://jekyllrb.com/docs/datafiles/">data files</a>.
What’s specific to <code class="language-plaintext highlighter-rouge">minimal-mistakes</code> is that you
add these links to the nav bar by specifying a <code class="language-plaintext highlighter-rouge">main</code> key
in <code class="language-plaintext highlighter-rouge">navigation.yml</code>, as described here:
<a href="https://mmistakes.github.io/minimal-mistakes/docs/navigation/">https://mmistakes.github.io/minimal-mistakes/docs/navigation/</a>.
In the case of <code class="language-plaintext highlighter-rouge">academicpages</code>,
the links have the names of the specific sections
in the <code class="language-plaintext highlighter-rouge">navigation.yml</code> with the keys “publications”, “talks”, etc.</p>

<h2 id="coda-publishing-etc">Coda: publishing, etc.</h2>

<p>After making the changes just described, I had a site
with a structure similar to <code class="language-plaintext highlighter-rouge">academicpages</code>.</p>

<p>To actually publish the site, I still use a workflow similar to
what Jake Vanderplas uses for his blog:
<a href="https://github.com/jakevdp/jakevdp.github.io-source">https://github.com/jakevdp/jakevdp.github.io-source</a>
This centers around a Makefile with commands for <code class="language-plaintext highlighter-rouge">build</code> and <code class="language-plaintext highlighter-rouge">serve</code>.</p>

<p>The key thing that makes it easy to publish to Github Pages is to
use the <code class="language-plaintext highlighter-rouge">ghp-import</code> package, as suggested by the <code class="language-plaintext highlighter-rouge">pelican</code> docs:
<a href="https://docs.getpelican.com/en/latest/tips.html#project-pages">https://docs.getpelican.com/en/latest/tips.html#project-pages</a>.
Of course, <code class="language-plaintext highlighter-rouge">jekyll</code> gets special treatment on GitHub
so it’s kind of overkill,
but I still like having a separate repo that I just push the
.html and .css files to without thinking about it.</p>

<p>So this is how I have my site set up now!
Stay tuned for it to change suddenly,
because as a developer I love to spend hours
farting around with tools that let me avoid real work.
I’ll keep this post up for posterity anyway.</p>]]></content><author><name>David Aaron Nicholson</name><email>nicholdav@gmail.com</email></author><summary type="html"><![CDATA[Update 2022-12-17: I added a home page and a blog to my site, so I no longer use a set up exactly like the academicpages theme, but I’m leaving this post up to document the process (and because it’s currently my only post :sob:)]]></summary></entry></feed>